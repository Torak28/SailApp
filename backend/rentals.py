from database.create_objects_of_classes import create_rental


def create_rental(user_id, centre_id, gear_id, rent_amount, rent_start, rent_end):
        create_rental(user_id, gear_id, centre_id, rent_start, rent_end, rent_amount)
